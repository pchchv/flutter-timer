name: flutter_timer
description: "Timer app written on Dart with Flutter."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.1

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
