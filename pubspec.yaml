name: flutter_timer
description: "Timer app written on Dart with Flutter."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.1

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
